package constants

const (
	// APIVersion is the version of the API.
	APIVersion = "0.2.2"

	// MinimumClientVersion is the minimum version of the app, requests from older clients will be rejected by the server.
	MinimumClientVersion = "0.0.1"
)
